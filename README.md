# Note-Taker
# Problems i had During the codeing. 

# 1
# Database Testing Table: SQL Statements/Queries, Insert a new note  
# Pass/Fail 
# notes = c.fetchall()
# Fail 
# I fixed the failing problem by changing the codes placing because I placed it in the wrong posigen.  

# 2
# Database Testing Table: SQL Statements/Queries, Insert a new note 
# Pass/Fail 
# c.execute(..., (note,))
# Fail 
# I had to change the code itself by adding other elements to make it work.  

# 3
# CSS Testing Table: Styling, Add hover states to buttons 
# Pass/Fail 
# CSS and frontend styling 
# Fail 
# It was a simple problem which was that I typed it wrong so it failed.   

# 4
# Button testing: python code, Delete the latest note  
# Pass/Fail 
# c.execute(‘DELETE FROM notes WHERE rowid = ?’, (last_note[0],))
# Fail 
# I keep messing up the text inside the execute but I did figure it out at the end. 

# 5
# Program Testing Table: Routes/Functions, Adding a new note, Submit a form with note and email fields ,and Displays the new note on the homepage 
# Fail 
# It failed because the connection between the python and html files was wrong and the code had a typo. 




